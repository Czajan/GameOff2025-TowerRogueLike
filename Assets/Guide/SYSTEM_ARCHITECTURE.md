# System Architecture - Visual Overview

## 🏗️ System Component Map

```
┌─────────────────────────────────────────────────────────────┐
│                      GAME MANAGERS                          │
│                    (Single GameObject)                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ GameProgressionManager (Singleton)                  │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ • Currency (int)                                    │   │
│  │ • Current Defense Zone (0-2)                        │   │
│  │ • Base Timer (float)                                │   │
│  │ • IsInBase (bool)                                   │   │
│  │                                                      │   │
│  │ Events:                                             │   │
│  │ → OnCurrencyChanged                                 │   │
│  │ → OnEnteredBase  ──────────┐                        │   │
│  │ → OnExitedBase   ──────────┼────────┐               │   │
│  │ → OnBaseTimerUpdate        │        │               │   │
│  │ → OnZoneChanged            │        │               │   │
│  └────────────────────────────┼────────┼───────────────┘   │
│                               │        │                   │
│  ┌────────────────────────────▼────────▼───────────────┐   │
│  │ PlayerStats (Singleton)                             │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ • Move Speed (base + levels)                        │   │
│  │ • Max Health (base + levels)                        │   │
│  │ • Damage (base + levels)                            │   │
│  │ • Crit Chance (base + levels)                       │   │
│  │ • Crit Damage (base + levels)                       │   │
│  │ • Attack Range (base + levels)                      │   │
│  │                                                      │   │
│  │ Methods:                                            │   │
│  │ → UpgradeMoveSpeed() ───────────┐                   │   │
│  │ → UpgradeDamage()   ────────────┼──┐                │   │
│  │ → CalculateFinalDamage()        │  │                │   │
│  │ → RollCritical()                │  │                │   │
│  │ → ApplyStatsToPlayer() ─────────┼──┼────┐           │   │
│  └─────────────────────────────────┼──┼────┼───────────┘   │
│                                    │  │    │               │
│  ┌─────────────────────────────────┼──┼────┼───────────┐   │
│  │ WeaponSystem (Singleton)        │  │    │           │   │
│  ├─────────────────────────────────┼──┼────┼───────────┤   │
│  │ • Equipped Weapon (WeaponData)  │  │    │           │   │
│  │                                 │  │    │           │   │
│  │ Methods:                        │  │    │           │   │
│  │ → EquipWeapon()                 │  │    │           │   │
│  │ → GetDamageMultiplier() ────────┼──┼────┼──┐        │   │
│  │ → TryApplyWeaponEffect() ───────┼──┼────┼──┼─┐      │   │
│  └─────────────────────────────────┼──┼────┼──┼─┼──────┘   │
│                                    │  │    │  │ │          │
│  ┌─────────────────────────────────▼──▼────▼──▼─▼──────┐   │
│  │ UpgradeShop                                         │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ • Available Upgrades (UpgradeData[])                │   │
│  │ • Available Weapons (WeaponData[])                  │   │
│  │                                                      │   │
│  │ Methods:                                            │   │
│  │ → OpenShop() (on enter base)                        │   │
│  │ → TryPurchaseUpgrade() (spend currency)             │   │
│  │ → TryPurchaseWeapon() (spend currency)              │   │
│  │ → ExitShop() (start wave)                           │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎮 Player Component Flow

```
┌─────────────────────────────────────────────────────────────┐
│                         PLAYER                              │
│                   (Player GameObject)                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ PlayerController                                    │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ Reads from:                                         │   │
│  │ ← PlayerStats.GetMoveSpeed() ───────────────────────┼───┤
│  │                                                      │   │
│  │ Controls:                                           │   │
│  │ • Movement (WASD)                                   │   │
│  │ • Sprint (Shift)                                    │   │
│  │ • Jump (Space)                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ PlayerHealth                                        │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ Reads from:                                         │   │
│  │ ← PlayerStats.GetMaxHealth() ───────────────────────┼───┤
│  │                                                      │   │
│  │ Methods:                                            │   │
│  │ • TakeDamage(float)                                 │   │
│  │ • Heal(float)                                       │   │
│  │ • SetMaxHealth(float) ← from PlayerStats            │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ PlayerCombat                                        │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ Reads from:                                         │   │
│  │ ← PlayerStats.CalculateFinalDamage() ───────────────┼───┤
│  │ ← PlayerStats.RollCritical()                        │   │
│  │ ← WeaponSystem.GetDamageMultiplier() ───────────────┼───┤
│  │ ← WeaponSystem.GetRangeMultiplier()                 │   │
│  │                                                      │   │
│  │ On Attack:                                          │   │
│  │ 1. Calculate damage (base × weapon × crit)          │   │
│  │ 2. Apply damage to enemies                          │   │
│  │ 3. Trigger weapon effects ──────────────────────────┼───┤
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 👾 Enemy Component Flow

```
┌─────────────────────────────────────────────────────────────┐
│                         ENEMY                               │
│                   (Enemy Prefab)                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ EnemyAI                                             │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ • Chase player                                      │   │
│  │ • Attack when in range                              │   │
│  │ • Apply gravity properly                            │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ EnemyHealth                                         │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ Settings:                                           │   │
│  │ • Currency Reward = 25 (configurable)               │   │
│  │                                                      │   │
│  │ On Death:                                           │   │
│  │ → GameProgressionManager.AddCurrency(reward) ───────┼───┤
│  │ → Destroy enemy                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🏰 Base & Defense Zone Flow

```
┌────────────────────────────────────────────────────────────────┐
│                      WORLD SPACE                               │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌────────────────┐   ┌────────────────┐   ┌────────────────┐ │
│  │ DefenseZone_1  │   │ DefenseZone_2  │   │ DefenseZone_3  │ │
│  │  (Frontline)   │   │    (Middle)    │   │     (Base)     │ │
│  ├────────────────┤   ├────────────────┤   ├────────────────┤ │
│  │ Index: 0       │   │ Index: 1       │   │ Index: 2       │ │
│  │ Perks: 0%      │   │ Perks: +25%    │   │ Perks: +50%    │ │
│  │                │   │                │   │                │ │
│  │ Next Zone ─────┼───│→ Next Zone ────┼───│→ Next: None   │ │
│  │                │   │                │   │                │ │
│  │ Spawn here     │   │ Fallback here  │   │ Last stand     │ │
│  │ on wave start  │   │ if health low  │   │ here           │ │
│  └────────────────┘   └────────────────┘   └────────────────┘ │
│         ↑                                           ↑          │
│         │                                           │          │
│    [Enemies spawn                             [Base Area]     │
│     in radius]                                               │
│                                                                │
│                                           ┌─────────────────┐  │
│                                           │  Base_Gate      │  │
│                                           ├─────────────────┤  │
│                                           │ • Opens on      │  │
│                                           │   enter base    │  │
│                                           │ • Closes on     │  │
│                                           │   exit/timer    │  │
│                                           │ • Animated      │  │
│                                           │   movement      │  │
│                                           └─────────────────┘  │
│                                                    ↑            │
│                                           ┌────────┴────────┐  │
│                                           │  Base_Trigger   │  │
│                                           ├─────────────────┤  │
│                                           │ OnTriggerEnter: │  │
│                                           │ → OpenGate()    │  │
│                                           │ → OpenShop()    │  │
│                                           │ → StartTimer()  │  │
│                                           └─────────────────┘  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Game Loop State Machine

```
                    ┌─────────────────┐
                    │   GAME START    │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  Spawn in Base  │
                    │  (Gate Open)    │
                    └────────┬────────┘
                             │
                             ▼
        ┌────────────────────────────────────────┐
        │          IN BASE (Safe Zone)           │
        ├────────────────────────────────────────┤
        │ • Shop UI appears                      │
        │ • Timer starts (40s)                   │
        │ • Can buy upgrades/weapons             │
        │ • Time.timeScale = 0 (paused)          │
        │ • No enemies                           │
        └────┬───────────────────────┬────────────┘
             │                       │
             │ Player exits      OR  │ Timer expires
             ▼                       ▼
        ┌────────────────────────────────────────┐
        │         EXIT BASE EVENT                │
        ├────────────────────────────────────────┤
        │ • Close shop UI                        │
        │ • Close gate (animation)               │
        │ • Time.timeScale = 1 (resume)          │
        │ • Trigger wave start                   │
        └────────────────┬───────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────────┐
        │       WAVE IN PROGRESS                 │
        ├────────────────────────────────────────┤
        │ • Enemies spawn at active zone         │
        │ • Player fights                        │
        │ • Earn currency per kill               │
        │ • If health < 25% → Fallback to next   │
        │   zone (teleport + zone perks)         │
        └────────────────┬───────────────────────┘
                         │
                         │ All enemies defeated
                         ▼
        ┌────────────────────────────────────────┐
        │        WAVE COMPLETE                   │
        ├────────────────────────────────────────┤
        │ • Calculate bonus (based on zone held) │
        │ • Award currency                       │
        │ • Open gate (animation)                │
        │ • Player returns to base               │
        └────────────────┬───────────────────────┘
                         │
                         │ Enter base trigger
                         ▼
        ┌────────────────────────────────────────┐
        │      BACK IN BASE (Repeat)             │
        │                                        │
        │ Wave difficulty increases              │
        │ Stats carry over                       │
        │ Weapons equipped persist               │
        └────────────────────────────────────────┘
             │
             └─── Loop continues ───┐
                                    │
                         ┌──────────▼──────────┐
                         │   GAME OVER         │
                         │ (Zone 3 lost)       │
                         └─────────────────────┘
```

---

## 💰 Currency & Upgrade Flow

```
┌──────────────────────────────────────────────────────────┐
│                   CURRENCY SYSTEM                        │
└──────────────────────────────────────────────────────────┘

    Kill Enemy
        │
        ▼
    [EnemyHealth.Die()]
        │
        │ currencyReward = 25
        ▼
    GameProgressionManager.AddCurrency(25)
        │
        ├──→ Currency += 25
        ├──→ OnCurrencyChanged.Invoke(newAmount)
        │        │
        │        └──→ [UI Updates] (future)
        │
        ▼
    Currency Pool: 150
        │
        │ Player in shop
        ▼
    Click "Upgrade Move Speed" Button
        │
        ▼
    UpgradeShop.TryPurchaseUpgrade(data, currentLevel)
        │
        ├──→ cost = data.GetCostForLevel(0) = 50
        │
        ├──→ GameProgressionManager.SpendCurrency(50)
        │        │
        │        ├──→ If currency >= 50: TRUE
        │        │    Currency -= 50
        │        │    return true
        │        │
        │        └──→ If currency < 50: FALSE
        │             return false
        │
        ▼ (if successful)
    PlayerStats.UpgradeMoveSpeed()
        │
        ├──→ moveSpeedLevel++
        ├──→ OnStatsChanged.Invoke()
        │
        └──→ ApplyStatsToPlayer()
                 │
                 ├──→ newSpeed = baseMoveSpeed + (level * perLevel)
                 │              = 5.0 + (1 * 0.5) = 5.5
                 │
                 └──→ PlayerController.SetMoveSpeed(5.5)
                          │
                          └──→ Player moves faster!

    Next upgrade costs MORE:
    Level 1 → 50 currency
    Level 2 → 75 currency (50 * 1.5)
    Level 3 → 112 currency (75 * 1.5)
    ...exponential scaling
```

---

## ⚔️ Combat Damage Calculation

```
┌──────────────────────────────────────────────────────────┐
│               ATTACK DAMAGE FLOW                         │
└──────────────────────────────────────────────────────────┘

    Player clicks attack
        │
        ▼
    [PlayerCombat.PerformAttack()]
        │
        ├──→ Get base damage from PlayerStats
        │    finalDamage = PlayerStats.CalculateFinalDamage()
        │        │
        │        ├──→ baseDamage = 10 (base)
        │        ├──→ + damageLevel * 5 = 15 (if level 1)
        │        │    = 25 total
        │        │
        │        ├──→ Roll crit? RollCritical()
        │        │    Random.value < critChance (0.05 + 0.05)
        │        │    = 10% chance
        │        │
        │        └──→ If crit: 25 * (1.5 + 0.25) = 43.75
        │             If not:  25
        │
        ├──→ Apply weapon multiplier
        │    finalDamage *= WeaponSystem.GetDamageMultiplier()
        │    43.75 * 1.2 (Fire Blade) = 52.5
        │
        ├──→ Apply to all enemies in range
        │    foreach enemy in OverlapSphere:
        │        enemy.TakeDamage(52.5)
        │
        └──→ Try weapon effects
             WeaponSystem.TryApplyWeaponEffect(enemy)
                 │
                 ├──→ If weapon.effect == Burn
                 │    AND Random.value < effectChance (0.3)
                 │    
                 └──→ Apply burn DOT to enemy
                      (5 damage per second for 3 seconds)

    Final damage: 52.5 + Burn effect
    Enemy dies faster! Currency earned!
```

---

## 📊 Data Asset Structure

```
PROJECT WINDOW

/Assets
  /Scripts
    /Systems
      ├── GameProgressionManager.cs
      ├── PlayerStats.cs
      ├── WeaponSystem.cs
      ├── UpgradeShop.cs
      ├── WeaponData.cs (ScriptableObject Template)
      └── UpgradeData.cs (ScriptableObject Template)
  
  /Data (Create this folder)
    /Upgrades
      ├── Upgrade_MoveSpeed.asset    [UpgradeData]
      ├── Upgrade_MaxHealth.asset    [UpgradeData]
      ├── Upgrade_Damage.asset       [UpgradeData]
      ├── Upgrade_CritChance.asset   [UpgradeData]
      ├── Upgrade_CritDamage.asset   [UpgradeData]
      └── Upgrade_AttackRange.asset  [UpgradeData]
    
    /Weapons
      ├── Weapon_BasicSword.asset    [WeaponData]
      ├── Weapon_FireBlade.asset     [WeaponData]
      ├── Weapon_IceSword.asset      [WeaponData]
      └── Weapon_StormHammer.asset   [WeaponData]

SCENE HIERARCHY

SampleScene
  ├── GameManagers (Empty GameObject)
  │   ├── GameProgressionManager
  │   ├── PlayerStats
  │   ├── WeaponSystem
  │   └── UpgradeShop
  │       ├─→ Available Upgrades: Array[6]
  │       └─→ Available Weapons: Array[4]
  │
  ├── Player
  │   ├── PlayerController (modified)
  │   ├── PlayerHealth (modified)
  │   └── PlayerCombat (modified)
  │
  ├── DefenseZone_1
  │   ├── DefenseZone (Index=0, Perks=0%)
  │   └── SpawnCenter (child Transform)
  │
  ├── DefenseZone_2
  │   ├── DefenseZone (Index=1, Perks=25%)
  │   └── SpawnCenter
  │
  ├── DefenseZone_3 (Base)
  │   ├── DefenseZone (Index=2, Perks=50%)
  │   └── SpawnCenter
  │
  └── Base
      ├── Base_Ground (Cube with collider)
      ├── Base_Gate (Cube with BaseGate + collider)
      └── Base_Trigger (Cube with BaseTrigger + trigger)
```

---

## 🎯 Key Integration Points

### 1. Player → Stats
```csharp
// PlayerController.cs
float speed = PlayerStats.Instance.GetMoveSpeed();
```

### 2. Player → Weapon
```csharp
// PlayerCombat.cs
float damage = PlayerStats.Instance.CalculateFinalDamage();
damage *= WeaponSystem.Instance.GetDamageMultiplier();
```

### 3. Enemy → Currency
```csharp
// EnemyHealth.cs (on death)
GameProgressionManager.Instance.AddCurrency(currencyReward);
```

### 4. Shop → Stats
```csharp
// UpgradeShop.cs
if (GameProgressionManager.Instance.SpendCurrency(cost))
{
    PlayerStats.Instance.UpgradeMoveSpeed();
}
```

### 5. Base → Gate → Shop
```csharp
// BaseTrigger.cs (on player enter)
GameProgressionManager.Instance.EnterBase();
    → BaseGate.OpenGate()
    → UpgradeShop.OpenShop()
```

---

## 🔗 Event System Map

```
GameProgressionManager Events:
├── OnCurrencyChanged(int) → UI currency text
├── OnEnteredBase() 
│   ├→ BaseGate.OpenGate()
│   └→ UpgradeShop.OpenShop()
├── OnExitedBase()
│   ├→ BaseGate.CloseGate()
│   ├→ UpgradeShop.CloseShop()
│   └→ WaveController.StartNextWave()
├── OnBaseTimerUpdate(float) → UI timer text
└── OnZoneChanged(int) → UI zone indicator

PlayerStats Events:
└── OnStatsChanged() → UI stat displays

WeaponSystem Events:
└── OnWeaponChanged(WeaponData) → UI weapon display

UpgradeShop Events:
├── OnUpgradePurchased(UpgradeData) → VFX/SFX
└── OnWeaponPurchased(WeaponData) → VFX/SFX
```

---

**This architecture provides:**
- ✅ Clear separation of concerns
- ✅ Event-driven loose coupling
- ✅ Data-driven design (ScriptableObjects)
- ✅ Easy to extend and maintain
- ✅ Designer-friendly (Inspector-based)
- ✅ Scalable for future features
